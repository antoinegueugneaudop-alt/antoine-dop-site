# Antoine Gueugneau — Director of Photography website

Codex write test

Premium static portfolio site prepared for fast GitHub + Codex + Vercel deployment.

## Status

- Static site: `index.html`
- CSS: `assets/css/styles.css`
- JS showreel modal: `assets/js/main.js`
- Images: copied from the supplied ZIP, renamed for clean URLs
- Showreel video: not committed; Vimeo embed is used instead

## Local preview

Open `index.html` directly in a browser, or run:

```bash
python3 -m http.server 5173
```

Then open: `http://localhost:5173`

## Deploy recommendation

Use Vercel connected to GitHub for fastest delivery and automatic previews.

Settings:

- Framework Preset: Other
- Build Command: leave empty
- Output Directory: leave empty / root
- Production Branch: `main`

## Important content rule

Use only real stills / photograms. No AI recreation, no relighting, no smoothing, no invented images.
